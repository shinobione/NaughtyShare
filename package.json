{
  "name": "naughtyshare",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && wrangler dev",
    "dev:ui": "vite",
    "build": "vite build",
    "preview": "npm run build && wrangler dev",
    "check": "npm run build && wrangler deploy --dry-run --outdir .wrangler/dry-run",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "jose": "^6.2.8"
  },
  "devDependencies": {
    "vite": "^8.1.0",
    "wrangler": "^4.119.0"
  }
}
